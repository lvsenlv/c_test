FILE_DIR = $(shell cd .. && pwd)
OBJS_DIR = $(shell pwd)/obj
$(shell mkdir -p $(OBJS_DIR))

PROGRAM = test
CC = gcc
COMPILE = $(CC) -g -c -o
INCLUDES_DIR = -I$(FILE_DIR)/include \
                -I$(FILE_DIR)/include/3tcloud/
CFLAGS := $(INCLUDES_DIR)

export
